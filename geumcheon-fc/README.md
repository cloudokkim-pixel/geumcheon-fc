# 서울금천축구클럽 홈페이지 시안

Next.js 기반 반응형 랜딩 페이지입니다.

## 실행 방법

```bash
npm install
npm run dev
```

## 배포

GitHub에 업로드한 뒤 Vercel에서 Import 하면 바로 배포할 수 있습니다.

## 이미지 교체

현재 로고와 포스터는 외부 URL을 사용합니다.
필요하면 `src/components/geumcheon-homepage.tsx` 안의 `logoSrc`, `posterSrc` 값을 교체하세요.
